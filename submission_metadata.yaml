# Redrob Hackathon — Submission Metadata

# ============================================================================
# Team identity
# ============================================================================
team_name: "Caffeinated Coders"  # Used in leaderboard and announcements

primary_contact:
  name: "Abbas Hussain"
  email: "abbashussain0986@gmail.com"   # Used for all organizer communication
  phone: "+91-8688872664"        # Used for top-50 / top-10 outreach

team_members:
  - name: "Abbas Hussain"
    email: "abbashussain0986@gmail.com"
    role: "Team Lead"          # Optional, e.g. "Team Lead", "Backend", "Data", "ML Engineer", "Data Engineer"
  - name: "Shaikh Mohd Rehaan"
    email: "shaikhrehaan2006@gmail.com"
    role: "ML Engineer"
  - name: "Solomon Pattapu"
    email: "pattapusolomon89@gmail.com"
    role: "Data Engineer"
    
# ============================================================================
# Code and reproducibility
# ============================================================================
github_repo: "https://github.com/Rehaan-2006/AI_Candidate_Ranking_System"

sandbox_link: "https://colab.research.google.com/drive/1tjiTjqLIaPZXyG_SImQ-ik-4dNJZBtnK?usp=sharing"  

reproduce_command: "python rank.py --candidates ./candidates.jsonl --out ./submission.csv"

# ============================================================================
# Compute environment
# ============================================================================
compute:
  platform: "MacBook Air M1"
  cpu_cores: 8
  ram_gb: 8
  python_version: "3.13"
  os: "macOS"
  uses_gpu_for_inference: false          # Ranking step is pure CPU + numpy
  has_network_during_ranking: false      # All artifacts pre-loaded from cache/
  pre_computation_required: true         # Embeddings + features built in precompute.py
  pre_computation_time_minutes: 1 hour 5 minutes       # On Google Colab T4 GPU via redrob_precompute.ipynb
  # Note: pre-computation on local M1 CPU takes approximately 60-90 minutes.
  # The Colab notebook is provided to reproduce this step on GPU in ~10 minutes.

# ============================================================================
# AI tools declaration
# ============================================================================
ai_tools_used:
  - "Claude"

ai_usage_summary: |
  Our team maintained a strict "human-at-the-top-of-the-loop" approach throughout the project,
  managing the product vision and execution end-to-end. We utilized Claude (Anthropic) as an
  advanced digital helper and junior pair-programmer. While Claude assisted with brainstorming
  architecture design, drafting the scoring formula, and generating boilerplate code across pipeline
  modules, all core engineering decisions were driven by human insight. We guided the AI on key
  choices like embedding model selection (BGE-base), the two-phase pre-computation/ranking split,
  and honeypot detection logic. Every line of code generated was rigorously reviewed, debugged,
  and validated by our team to ensure it met our standards. We deliberately designed the system
  so that no candidate data or profile content is ever sent to any external LLM or API, guaranteeing
  complete data privacy. The final ranking step runs entirely offline, reflecting our commitment to
  building the right product the right way.

# ============================================================================
# Approach summary
# ============================================================================
methodology_summary: |
  Two-phase pipeline. Phase 1 (pre-computation, no time limit): all 100,000
  candidate profiles are converted to rich text representations prioritising
  career history descriptions over skills lists, then embedded using
  BAAI/bge-base-en-v1.5 (768-dim). The job description is distilled into a
  focused query embedding using BGE's asymmetric retrieval instruction prefix.
  Five structured feature scores are extracted per candidate: career quality
  (product-company ratio, AI/ML title progression, tenure stability), 
  availability (activity recency, open-to-work flag, response rate, notice
  period, interview completion), location (tiered against Pune/Noida
  preference), and skill depth (assessment-verified JD-relevant skills plus
  GitHub activity). A honeypot multiplier (0.05–1.0) flags structural
  impossibilities — education timeline paradoxes, proficiency inflation vs
  verified assessments, expert skills with zero duration. Phase 2 (ranking,
  <10 seconds on CPU): cosine similarity is computed as a single numpy matrix
  multiply. A weighted composite score (semantic 35%, career quality 25%,
  availability 20%, location 15%, skill depth 5%) is fused, the honeypot
  multiplier is applied, and top-100 candidates are selected with
  candidate_id tie-breaking. Reasoning is generated from literal profile
  fields — no LLM calls at ranking time.

# ============================================================================
# Declarations
# ============================================================================
declarations:
  read_submission_spec: true
  code_is_original_work: true
  no_collusion: true
  honeypot_check_done: true       
  reproduction_tested: true       